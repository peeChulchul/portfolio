import React from "react";
import SectionLayout from "section/common/section-layout";

export default function SectionProject({ index }) {
  return (
    <SectionLayout index={index} bg={"orange"}>
      SectionProject
    </SectionLayout>
  );
}
